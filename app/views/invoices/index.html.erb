<div class="row">
    <div class="col-md-12">
        <div>
            <%=datagrid_form_for @grid, :method=> :get, :url => invoices_path%>

                <%=paginate(@grid.assets)%>
                    <%=datagrid_table @grid%>
                        <%=paginate(@grid.assets)%>
        </div>
    </div>
</div>
